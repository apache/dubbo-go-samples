dubbo:
  registries:
    demoZK:
      protocol: nacos
      timeout: 3s
      address: dubbogo-nacos:8848
  protocols:
    triple:
      name: tri
      port: 20000
  provider:
    registryIDs:
      - demoZK
    services:
      GreeterProvider:
        protocolIDs: triple
        interface: com.apache.dubbo.sample.basic.IGreeter # must be compatible with grpc or dubbo-java
dubbo:
  protocols:
    triple:
      name: tri
      port: 20000
  provider:
    services:
      greeterImpl:
        protocol: triple
        interface: com.apache.dubbo.sample.basic.IGreeter # must be compatible with grpc or dubbo-java